defaults:
  - override hydra_logging: colorlog
  - override job_logging: colorlog

run:
  dir: ${paths.output_dir}

job_logging:
  handlers:
    file:
      filename: ${hydra.runtime.output_dir}/logs/${hydra.job.name}.log
